{
  "name": "leetcode-status-bff",
  "version": "0.1.0",
  "private": true,
  "description": "LeetCode submission status polling API (GET /submissions/{submissionId}/status). JWT-protected, read-only on the submissions table.",
  "main": "src/rest/handlers.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "package": "sls package",
    "deploy": "sls deploy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.705.0",
    "@aws-sdk/lib-dynamodb": "^3.705.0",
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.0",
    "serverless": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
