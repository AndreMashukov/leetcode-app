{
  "name": "leetcode-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Cognito UserPool for the leetcode system. One shared pool across all BFFs. Forked from jgilbert01/templates/template-control-service.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "package": "serverless package",
    "deploy": "serverless deploy",
    "info": "serverless info",
    "remove": "serverless remove",
    "logs": "serverless logs -f"
  },
  "devDependencies": {
    "serverless": "^4.0.0"
  }
}
