{
  "name": "leetcode-submissions-bff",
  "version": "0.1.0",
  "private": true,
  "description": "LeetCode submission ingest API (POST /problems/{slug}/submission, GET /me/submissions). JWT-protected HTTP API that persists submissions and hands them to workers via SQS.",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.12.7",
    "esbuild": "^0.21.5",
    "serverless": "^4.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.683.0",
    "@aws-sdk/client-sqs": "^3.683.0",
    "@aws-sdk/lib-dynamodb": "^3.683.0",
    "ulid": "^2.3.0"
  }
}
